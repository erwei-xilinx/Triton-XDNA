# Requirements for building triton-air wheels
#
# These dependencies are installed during cibuildwheel's before-build step

# Build tools
cmake>=3.20
ninja
setuptools>=61.0
wheel

# Python bindings
pybind11
nanobind

# Triton build dependencies
lit

# Testing (optional, for build verification)
pytest
numpy
